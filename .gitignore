# macOS metadata
._*
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp

# Vercel
.vercel/

# Local env files (never commit secrets)
.env
.env.local
.env.*.local

# Generated by scripts/inject-env.js when SUPABASE_URL / SUPABASE_ANON_KEY are set.
# Uncomment the next line if you decide to ONLY use env-injection and never want to
# commit your dev credentials in js/config.js.
# js/config.js

# Node (only used by scripts/inject-env.js; no runtime deps)
node_modules/
npm-debug.log*
